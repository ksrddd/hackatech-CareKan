DATABASE_URL="postgresql://carekan:carekan@localhost:5432/carekan?schema=public"
JWT_SECRET=""
PORT=4000
# Enforce the official Thai national-ID checksum on registration.
# Leave false for the demo (its fixed demo IDs aren't checksum-valid); set
# true in production so registrations require a genuine ID.
STRICT_NATIONAL_ID=false
